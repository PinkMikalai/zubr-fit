# MODÈLE — avant de lancer "npm run build" pour la prod, copie ceci dans un fichier
# ".env.production" (à la racine de frontend/, non commité) avec la vraie URL de l'API.
VITE_API_URL=https://api.zubr-fit.com
